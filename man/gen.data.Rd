% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Crossfit.R, R/datGen.R
\name{gen.data}
\alias{gen.data}
\title{Generate Synthetic Data}
\usage{
gen.data(n, do.pop = F, do.misp = F, my.transform = F, seed = 123)

gen.data(n, do.pop = F, do.misp = F, my.transform = F, seed = 123)
}
\arguments{
\item{n}{Integer. The number of observations to generate.}

\item{do.pop}{Logical. If \code{TRUE}, the function returns the counterfactual outcomes.}

\item{do.misp}{Logical. If \code{TRUE}, the function applies a transformation to the true covariates.}

\item{my.transform}{Logical. If \code{TRUE} and \code{do.misp} is \code{TRUE}, the function uses the Balzer & Westling transformation. Otherwise, it uses the Naimi et al. transformation.}

\item{seed}{Integer. The random seed for reproducibility.}
}
\value{
A data frame with the generated data. If \code{do.pop} is \code{TRUE}, it contains the propensity score and counterfactual outcomes.
If \code{do.misp} is \code{TRUE}, the covariates may be transformed.

A data frame with the generated data.
}
\description{
The \code{gen.data} function generates synthetic datasets for simulation studies.
It can produce observed data and counterfactual outcomes based on specified parameters.

The \code{gen.data} function generates synthetic datasets for demonstrating the application of \code{Crossfit} package.
It can produce observed data and counterfactual outcomes based on specified parameters.
}
\details{
The function generates four covariates (C1, C2, C3, C4) from standard normal distributions.
A propensity score is calculated using these covariates, and a binary treatment indicator (X) is sampled.
The outcome (Y) is generated based on the covariates and treatment.

If \code{do.pop} is \code{TRUE}, the function returns the propensity score and counterfactual outcomes (Y1, Y0).
If \code{do.misp} is \code{TRUE}, it applies transformations to the covariates before generating the outcome.

The function generates four covariates (C1, C2, C3, C4) from standard normal distributions.
A propensity score is calculated using these covariates, and a binary treatment indicator (X) is sampled.
The continuous outcome (Y) is generated based on the covariates and treatment.

If \code{do.pop} is \code{TRUE}, the function returns the counterfactual outcomes (Y1, Y0).
If \code{do.misp} is \code{TRUE}, it applies transformations to the true covariates.
}
\examples{
# Generate a dataset with 100 observations, including counterfactual outcomes
data <- gen.data(n = 100, do.pop = TRUE)

# Generate a dataset with transformed covariates
data_misp <- gen.data(n = 100, do.misp = TRUE, my.transform = TRUE)

# Generate a dataset with 100 observations, including counterfactual outcomes
data <- gen.data(n = 100, do.pop = TRUE)

# Generate a dataset with transformed covariates
data_misp <- gen.data(n = 100, do.misp = TRUE, my.transform = TRUE)

}
